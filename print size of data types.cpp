#include<stdio.h>
main()
{
	printf("%d%d%d%d%d%d%d%d",size of(char),size of(int),size of(float),size of (double),size of(short int),size of(unsigned charm),size of (signed int),size of (signed int));
		
}
